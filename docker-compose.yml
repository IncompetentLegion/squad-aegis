services:
  database:
    image: postgres:18-alpine
    ports:
      - 5432:5432
    volumes:
      - database:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=squad-aegis
      - POSTGRES_PASSWORD=squad-aegis
      - POSTGRES_DB=squad-aegis
      - PGDATA=/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U squad-aegis"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  clickhouse:
    image: clickhouse/clickhouse-server:25.3
    ports:
      - 8123:8123
      - 9000:9000
    volumes:
      - clickhouse:/var/lib/clickhouse
    environment:
      CLICKHOUSE_USER: "squad_aegis"
      CLICKHOUSE_PASSWORD: "squad_aegis"
    healthcheck:
      test: ["CMD", "clickhouse-client", "--query", "SELECT 1"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped

  valkey:
    image: valkey/valkey:8.1.3
    ports:
      - 6379:6379
    volumes:
      - valkey:/data
    restart: unless-stopped

  server:
    image: ghcr.io/incompetentlegion/squad-aegis:latest
    depends_on:
      - database
      - clickhouse
      - valkey
    ports:
      - 3113:3113
    volumes:
      - data:/etc/squad-aegis
    env_file:
      - .env
    restart: unless-stopped

volumes:
  database:
  clickhouse:
  data:
  valkey:
